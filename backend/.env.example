# ── Server ────────────────────────────────────────────────────
NODE_ENV=development
PORT=5000

# ── MongoDB Atlas ─────────────────────────────────────────────
# 1. Go to https://cloud.mongodb.com
# 2. Create a free cluster (M0)
# 3. Create a DB user under Security > Database Access
# 4. Allow your IP under Security > Network Access (or 0.0.0.0/0 for all)
# 5. Click "Connect" > "Drivers" and copy the connection string
# 6. Replace <username>, <password>, and <dbname> below
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/smart-canteen?retryWrites=true&w=majority

# ── JWT ───────────────────────────────────────────────────────
# Use a long random string — at least 32 characters
JWT_SECRET=change_this_to_a_long_random_secret_minimum_32_chars
JWT_EXPIRES_IN=7d

# ── CORS / Client URL ─────────────────────────────────────────
# In development: http://localhost:3000
# In production:  https://your-deployed-frontend.com
CLIENT_URL=http://localhost:3000
